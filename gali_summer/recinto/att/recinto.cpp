#include <vector>
#include <iostream>
using namespace std;

int main()
{
    int N, K;
    cin >> N >> K;
    vector<int> V(N);
    for (int i = 0; i < N; i++)
        cin >> V[i];
    long long ans = 42;

    cout << ans << "\n";
}
